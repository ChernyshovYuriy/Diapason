<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="true" />

    <!-- Used only to deliver the optional "Re-test in 30 days" reminder.
         Android 13+ requires a runtime grant; earlier versions get it implicitly. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission
        android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove" />

    <uses-permission
        android:name="android.permission.ACCESS_ADSERVICES_AD_ID"
        tools:node="remove" />

    <uses-permission
        android:name="android.permission.ACCESS_ADSERVICES_ATTRIBUTION"
        tools:node="remove" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".MainApp"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Diapason">

        <!-- Firebase auto-initializes both SDKs via a ContentProvider that runs before
             MainApp.onCreate(), and both collect by default. These flags flip that default
             to off so nothing is collected until the user agrees to the privacy policy in
             the first-launch consent dialog, which re-enables collection explicitly
             (see PrivacyConsentGate / MainApp.setCollectionEnabled). -->
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Disable WorkManager's automatic androidx.startup initialization. It runs as a
             ContentProvider.onCreate() during handleBindApplication, before MainApp.onCreate()
             — on some Android 14 device firmwares whose framework.jar is missing
             JobScheduler.forNamespace(String) despite reporting SDK_INT 34, that unconditional
             call throws NoSuchMethodError there and kills the process unrecoverably on every
             launch (see WorkManagerSupport). Initializing manually from MainApp.onCreate lets
             that failure be caught and confined to the reminder feature instead. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
